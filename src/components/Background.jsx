import { Neat } from "./Design/Neat";

const Background = () => {
      return (
            <section className="w-full overflow-hidden h-dvh">
                  <Neat />
            </section>
      );
};

export default Background;
